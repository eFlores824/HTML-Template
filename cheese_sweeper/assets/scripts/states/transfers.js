function goToGame(evt) {
	switchStates(gameState);
}

function goToInstruction(evt) {
	switchStates(instructionState);
}

function goToTitle(evt) {
	switchStates(titleState);
}

function goToGameOver(evt) {
	switchStates(gameoverState);
}

function goToCongrats(evt) {
	switchStates(congratsState);
}